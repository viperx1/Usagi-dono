name: Windows Build & Release (Qt 6.9.2 LLVM MinGW)

on:
  push:
    branches:
      - master
      - dev
    tags:
      - 'v*.*.*'
      - 'v*.*.*-beta*'

permissions:
  contents: read
  issues: write

jobs:
  build-llvm-mingw:
    runs-on: windows-latest
    name: Build with LLVM/Clang (Qt 6.9.2 MinGW)

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Qt 6.9.2 (LLVM MinGW)
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.9.2'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_llvm_mingw'
          cache: true

      - name: Install CMake and Ninja
        uses: lukka/get-cmake@latest

      # Capture logs for configure, build, and test
      - name: Configure, build, and test
        run: |
          # Set compiler to Clang from Qt LLVM MinGW installation
          # The Qt installation includes Clang/LLVM which supports PDB generation
          # QT_ROOT_DIR is set by install-qt-action to the Qt installation directory
          
          # Convert Windows path to Unix-style path for bash
          QT_BIN_DIR=$(cygpath -u "${QT_ROOT_DIR}/bin")
          
          if [ -d "${QT_BIN_DIR}" ] && [ -f "${QT_BIN_DIR}/clang++.exe" ]; then
            export PATH="${QT_BIN_DIR}:$PATH"
            export CC="${QT_BIN_DIR}/clang.exe"
            export CXX="${QT_BIN_DIR}/clang++.exe"
            echo "Using Clang from Qt: ${CXX}"
            
            # Convert paths back to Windows format for CMake
            CMAKE_C_COMPILER=$(cygpath -w "${CC}")
            CMAKE_CXX_COMPILER=$(cygpath -w "${CXX}")
            
            cmake -G "Ninja" -S . -B build -DCMAKE_BUILD_TYPE=Release \
              -DCMAKE_C_COMPILER="${CMAKE_C_COMPILER}" \
              -DCMAKE_CXX_COMPILER="${CMAKE_CXX_COMPILER}" 2>&1 | tee build_log.txt
          else
            echo "Warning: Qt LLVM MinGW bin directory not found or clang++.exe not present"
            echo "QT_ROOT_DIR=${QT_ROOT_DIR}"
            echo "Contents of Qt directory:"
            ls -la "${QT_ROOT_DIR}" || true
            [ -d "${QT_ROOT_DIR}/bin" ] && ls -la "${QT_ROOT_DIR}/bin" || echo "bin directory not found"
            echo "Using default compiler"
            cmake -G "Ninja" -S . -B build -DCMAKE_BUILD_TYPE=Release 2>&1 | tee build_log.txt
          fi
          
          cmake --build build --config Release 2>&1 | tee -a build_log.txt
          ctest --output-on-failure --test-dir build 2>&1 | tee -a build_log.txt
        shell: bash

      - name: Upload build artifact (Alpha, LLVM)
        if: success() && github.event_name == 'push' && startsWith(github.ref, 'refs/heads/')
        uses: actions/upload-artifact@v4
        with:
          name: Usagi-dono-alpha-llvm
          path: |
            build/**/*.exe
            build/**/*.pdb

      - name: Create Release & Upload (Stable/Beta, LLVM)
        if: success() && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/usagi/*.exe
            build/usagi/*.pdb
            build/tests/*.exe
            build/tests/*.pdb

      # 👇 The magic failure handler
      - name: Create issue with build/test logs on failure
        if: failure()
        run: |
          echo "🚨 **Build failed for workflow:** ${{ github.workflow }}" > issue_body.md
          echo "" >> issue_body.md
          echo "**Branch/Tag:** ${{ github.ref }}" >> issue_body.md
          echo "**Commit:** ${{ github.sha }}" >> issue_body.md
          echo "" >> issue_body.md
          echo "### Logs (truncated to last 500 lines)" >> issue_body.md
          echo '```' >> issue_body.md
          tail -n 500 build_log.txt >> issue_body.md
          echo '```' >> issue_body.md
          echo "" >> issue_body.md
          echo "[View full logs here](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> issue_body.md

          gh issue create \
            --title "🚨 Build failed: ${{ github.workflow }} on ${{ github.ref }}" \
            --body-file issue_body.md \
            --label ci-failure,bug
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
